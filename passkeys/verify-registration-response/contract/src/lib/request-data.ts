import {
  RegistrationResponseJSON,
  RegistrationResponseJSON as _RegistrationResponseJSON,
} from '@firebase-with-passkeys/passkeys-types';

export type RequestData = RegistrationResponseJSON;

export const RequestData = _RegistrationResponseJSON;
